# ┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
# ┃                     Defaults Configuration                  ┃
# ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

# $filemanager = ghostty -e "export EDITOR=nvim;yazi"
$filemanager = thunar
$applauncher = pkill rofi || rofi -show drun
$windowswitcher = pkill rofi || rofi -show window
$terminal = kitty
$idlehandler = swayidle -w timeout 300 'swaylock -f -c 000000' before-sleep 'swaylock -f -c 000000'
$capturing = grimblast --freeze --notify copy area
$browser = firefox
